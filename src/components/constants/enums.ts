enum Direction {
    ASCENDING,
    DECENDING,
}

enum Status {
    RESOLVED,
    PENDING,
    NEW
}

enum ModalType {
    SUCCESS = 'SUCCESS',
    ERROR = 'ERROR',
    WARNING = 'WARNING'
}

enum LanguageTypes {
    EST = 'EST',
    ENG = 'ENG',
}

export {
    Direction,
    Status,
    ModalType,
    LanguageTypes
};